import React from 'react'

const UserList = ({usuarios}) => {
    return (
        <>
            {usuarios.map(paciente=>(
                
            ))}
        </>
    );
}

export default UserList